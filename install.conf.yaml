- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.anyconnect.xml: anyconnect.xml
    ~/.bash_aliases: bash_aliases
    ~/.bashrc: bashrc
    ~/.condarc: condarc
    #~/.gitattributes: gitattributes
    #~/.gitignore_global: gitignore_global
    ~/.latexmkrc: latexmkrc
    ~/.profile: profile
    ~/.vimrc: vimrc
