- shell:
    - [add-apt-repository universe, Enabling the Universe APT repository]

# Dependencies to install Docker repository and package.
- apt-get:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg-agent
    - software-properties-common

- shell:
    - ['if ! grep docker /etc/apt/sources.list /etc/apt/sources.list.d/*; then curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add - && UBUNTU_VERSION=$(lsb_release -cs) && add-apt-repository --no-update "deb [arch=amd64] https://download.docker.com/linux/ubuntu ${UBUNTU_VERSION/eoan/disco} stable"; fi', Adding the Docker APT repository]

- apt-get:
    - aptitude  # To check the high-level installed packages.
    - aspell-en
    - aspell-es
    - base-passwd
    - bridge-utils
    - build-essential
    - calibre
    - cmake
    - console-setup
    - containerd.io
    - context
    - cpu-checker
    - dash
    - default-jdk
    - diffutils
    - docker-ce
    - docker-ce-cli
    - dos2unix
    - e2fsprogs
    - findutils
    - firefox
    - fonts-firacode
    - fonts-indic
    - git
    - gparted
    - grep
    - grub-common
    - grub-efi-amd64-signed
    - grub-gfxpayload-lists
    - grub-pc
    - grub-pc-bin
    - grub2-common
    - gstreamer1.0-pocketsphinx
    - gstreamer1.0-qt5
    - guake
    - gzip
    - hostname
    - htop
    - hunspell-en-us
    - hyphen-en-us
    - init
    - keyboard-configuration
    - language-pack-en
    - language-pack-en-base
    - language-pack-gnome-en
    - language-pack-gnome-en-base
    - libbz2-dev
    - libdebconfclient0
    - libffi-dev
    - liblzma-dev
    - libncurses5-dev
    - libpulse-dev
    - libreadline-dev
    - libreoffice
    - libsqlite3-dev
    - libssl-dev
    - libxml2-dev
    - libxmlsec1-dev
    - llvm
    - locales
    - make
    - mlocate
    - mokutil
    - mythes-en-us
    - ncurses-base
    - ncurses-bin
    - network-manager-openconnect-gnome
    - os-prober
    - pciutils
    - poppler-data
    - python3-gpg
    - python3-pip
    - python3-venv
    - qemu-kvm
    - shellcheck
    - shim-signed
    - telegram-desktop
    - texlive*
    - tk-dev
    - tmux
    - tree
    - udev
    - usbutils
    - vim
    - wamerican
    - wbritish
    - wireshark-qt
    - xclip
    - xz-utils
    - zlib1g-dev

- shell:
    # Can't set the release to latest for some because the download asset has the version name in it.
    - [TEMP_DEB=$(mktemp) && wget -O "$TEMP_DEB" 'https://github.com/keeweb/keeweb/releases/download/v1.12.3/KeeWeb-1.12.3.linux.x64.deb' && dpkg --skip-same-version -i "$TEMP_DEB" && rm -f "$TEMP_DEB", Installing KeeWeb]
    - [apt --fix-broken -y install, Installing KeeWeb dependencies]
    - [TEMP_DEB=$(mktemp) && wget -O "$TEMP_DEB" 'https://github.com/cli/cli/releases/download/v0.5.7/gh_0.5.7_linux_386.deb' && dpkg --skip-same-version -i "$TEMP_DEB" && rm -f "$TEMP_DEB", Installing gh]
    - [apt --fix-broken -y install, Installing gh dependencies]
    - [TEMP_DEB=$(mktemp) && wget -O "$TEMP_DEB" 'https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb' && dpkg --skip-same-version -i "$TEMP_DEB" && rm -f "$TEMP_DEB", Installing Google Chrome]
    - [apt --fix-broken -y install, Installing Google Chrome dependencies]
    - [TEMP_DEB=$(mktemp) && wget -O "$TEMP_DEB" 'https://github.com/KryDos/todoist-linux/releases/download/1.19/Todoist_1.19.0_amd64.deb' && dpkg --skip-same-version -i "$TEMP_DEB" && rm -f "$TEMP_DEB", Installing Todoist]
    - [apt --fix-broken -y install, Installing Todoist dependencies]

- shell:
    - [snap install --classic android-studio, Installing Android Studio]
    - [snap install --classic clion, Installing CLion]
    - [snap install --classic goland, Installing GoLand]
    - [snap install --classic intellij-idea-ultimate, Installing IntelliJ IDEA Ultimate]
    - [snap install musescore, Installing Musescore]
    - [snap install pdftk, Installing pdftk]
    - [snap install --classic pycharm-professional, Installing PyCharm Professional]
    - [snap install --classic rubymine, Installing RubyMine]
    - [snap install --classic skype, Installing Skype]
    - [snap install --classic slack, Installing Slack]
    - [snap install spotify, Installing Spotify]
    - [snap install --classic sublime-text, Installing Sublime]
    - [snap install --classic vscode, Installing VSCode]
    - [snap install --classic webstorm, Installing WebStorm]
